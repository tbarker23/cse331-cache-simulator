#include <iostream>
#include <fstream>

using namespace std;

int main( int argc, char** argv )
{
    /* Check for proper usage */
    if( argc != 3 )
    { 
        cout<< "Usage:\n\t";
        cout<< "cachesim configFile traceFile\n";
        return -1;
    }

    /* Open config file and glean configuration */

    /* SIMULATE... THAT... CACHE!!!! */

    return 0;
}
